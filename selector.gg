data (select row_number as id, * from '../data/hmda/2023_combined_mlar.parquet') hmda
stream (select hmda.* from hmda where hmda.id = amt.id) price_range 

dot
    x hmda.loan_amount
    y hmda.rate_spread
    param "amt" x

table
    row $ROW
    col $COL
